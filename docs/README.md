# docs

| file | what it is |
|---|---|
| `ko-fi-button.svg` | the Ko-fi button used in the README header |
| `social-preview.png` | 1280×640 GitHub social preview — upload it under Settings → Social preview |
| `star-history/` | star chart, regenerated in-repo by `.github/workflows/star-history.yml` |

The social preview is itself a chart-forge render (the headline beside a live `line` chart), so it
stays in step with the palette and the type scale.
